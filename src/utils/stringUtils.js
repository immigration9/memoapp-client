/**
 * JSON Stringify
 * @param { object } data
 */
export function strfy(data) {
  return JSON.stringify(data);
}

/**
 * String Empty check
 */
export function isEmptyString(str) {
  return str.length === 0;
}
